require('babel-polyfill')
// const chai = require('chai')
// const expect = chai.expect
// const mongo = require('../../lib/mongo.js')

describe('Test template', () => {
  before('Build up test data', async() => {
    // await mongo script
  })
  after('Clean up test data', async() => {
    // await mongo script
  })
  it('Can create something', async() => {
    // expect(result.status).to.be.true
  })
  it('Can query something', async() => {
    // expect(result.data[0].id).to.equal(id)
  })
})
